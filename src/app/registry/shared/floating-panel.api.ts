// GENERATED FILE — do not edit by hand. Run `npm run api-docs` to regenerate.
import type { ComponentApiDoc } from '../../docs-ui/api-table/api-table';

export const floatingPanelApi: ComponentApiDoc = {
  name: "FloatingPanel",
  description: "Shared CDK-Overlay wrapper for \"anchor a floating panel to a trigger element\" — flip/shift positioning, closes on outside click or Escape, repositions on scroll. Add to a component's own `providers: [FloatingPanel]` (not `providedIn: 'root'`) so each component instance gets its own. `Popover` is the generic (trigger + projected content) consumer of this. Combobox/Autocomplete/ Command use it directly since they need tighter control over their own input+listbox markup.",
  props: [

  ],
};
